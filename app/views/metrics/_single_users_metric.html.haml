Single User Metrics
%ul
  - collection.each do |metric|
    %li
      %p{class: "metric-key-#{metric.vars_for_render[:name]}"}
        %strong= metric.vars_for_render[:friendly_name]
        %ul
          - metric.vars_for_render[:data].each do |pair|
            %li
              = link_to "#{pair[:resource].name}", resource_overview_path(pair[:resource].username)
              = pair[:amount]